import type { MetadataRoute } from "next";
import { headers } from "next/headers";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const requestHeaders = await headers(); const host = requestHeaders.get("x-forwarded-host") ?? requestHeaders.get("host") ?? "localhost:3000"; const protocol = requestHeaders.get("x-forwarded-proto") ?? (host.startsWith("localhost") ? "http" : "https"); const origin = `${protocol}://${host}`; const primary = new Set(["", "/course", "/about", "/stories", "/resources", "/contact"]); return ["", "/course", "/about", "/stories", "/resources", "/contact", "/privacy", "/terms", "/refund", "/disclaimer"].map((path) => ({ url: `${origin}${path}`, lastModified: new Date(), changeFrequency: path === "" || path === "/resources" ? "weekly" as const : "monthly" as const, priority: path === "" ? 1 : primary.has(path) ? .75 : .3 })); }
